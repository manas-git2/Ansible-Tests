---
- name: Ansible playbook to test ansible vault
  hosts: all
  become: true
  vars_files:
    - vault-pass.yml
  tasks:
  - name: clone a repo
    git:
      repo: https://manas-git2:{{ password }}@github.com/manas-git2/Vault.git
      dest: /opt/ansadmin/test-vault
  

